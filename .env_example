export BOT_TOKEN=
export CRYPTO_TOKEN=
export REGOLO_API_KEY=
export REGOLO_MODEL=gpt-oss-120b
export ADMIN_DISCORD_ID=
export APP_ID=

# Optional: remote MCP server (e.g. baki) exposed over Streamable HTTP.
# When set, its tools are registered into the bot's toolbelt.
export BAKI_MCP_URL=
export BAKI_MCP_TOKEN=

# Required only for per-user OAuth MCP servers: passphrase used to encrypt
# stored OAuth tokens at rest (any string; hashed to a 32-byte key).
export TOKEN_ENCRYPTION_KEY=